# `/review` コマンド 使い方ガイド

## 概要

`/review` は PR の変更内容をレビューする Claude Code スラッシュコマンドです。

**code(実装コード)と design(設計ドキュメント)の 2 モード** と、両方を含むハイブリッド PR 用の **ハイブリッドモード(全 10 視点独立)** を備え、変更内容に応じて自動判定または明示指定でレビュー視点を切り替えます。AI 支援開発(Claude Code 等)で生成された成果物に特有のリスク(ハルシネーション・セキュリティ不備・ビジネス意図の誤解・段階間整合性崩壊)を多角的にレビューします。

本ルールは特定のワークフロー固有名詞に依存せず、**抽象語彙**(上流文書 / 下流文書 / マスター文書 / 派生文書)で記述されています。本リポジトリの AIDLC ワークフローへの具象対応は `.claude/commands/review.md` 末尾の「AIDLC マッピング」セクションを参照してください。

### 信頼性を担保する 6 つの仕組み

| # | 仕組み | 目的 |
|---|--------|------|
| 1 | カバレッジ宣言(出力先頭で必須) | 何を読み / 読まなかったかを透明化、`✅ 問題なし` の信頼性確保 |
| 2 | 信頼度軸(🎯 / 🤔 / ❓)を必須付与 | 断定指摘と仮説指摘を分離、ハルシネーション Critical の誤マージブロックを防ぐ |
| 3 | 各指摘に根拠視点・根拠項目を必須引用 | 視点カバレッジの検算とルール側の項目改善信号 |
| 4 | メタフィードバック節(出力末尾で必須) | ルール自身を実運用フィードバックで進化させる自己改善ループ |
| 5 | PR 規模・形態判定(空 / リバート / 巨大) | 言語非依存の普遍エッジケース対応 |
| 6 | `--since` の `🆕 New / 🙇 Missed / 🔄 Resurfaced` 分類 | 再レビュー時に前回見落としを自己申告、レビュアー品質指標として独立価値 |

---

## 設計の参考元

| 出典 | 採用した観点 |
|---|---|
| [Google Engineering Practices](https://google.github.io/eng-practices/review/) | 設計・複雑性・命名・テスト・一貫性の基準 |
| [mgreiler/code-review-checklist](https://github.com/mgreiler/code-review-checklist) | 12 カテゴリの包括的チェックリスト構造 |
| [axolo-co/developer-resources](https://github.com/axolo-co/developer-resources/blob/main/code-review-guideline-template/code-review-guideline-template.md) | ガイドラインテンプレートのフレームワーク |
| [Vibe Coding Security - Checkmarx](https://checkmarx.com/blog/security-in-vibe-coding/) | AI 生成コード特有の脆弱性パターン |
| [AI-Generated Code Vulnerabilities - Vidoc Security Lab](https://blog.vidocsecurity.com/blog/vibe-coding-security-vulnerabilities) | SQLi・XSS・認証不備など 9 種の具体的脆弱性 |
| [AI Code Review Best Practices - Graphite](https://graphite.com/guides/ai-code-review-implementation-best-practices) | AI 開発フロー向けレビュー実装指針 |
| [Establishing Standards for AI-Generated Code - MetaCTO](https://www.metacto.com/blogs/establishing-code-review-standards-for-ai-generated-code) | AI 生成コード専用のレビュー基準 |
| プロジェクト内検出事例(PR #3 Application Design レビュー) | 設計ドキュメント特有のハルシネーション検出パターン・段階間整合性視点 |

---

## 使い方

### 基本的な使い方

PR ブランチにチェックアウトした状態で、Claude Code のチャットに入力します:

```
/review
```

Claude が以下の順で自動実行します:
1. **モード自動判定**(code / design / ハイブリッド)
2. **PR 規模・形態の判定**(空 PR / リバート / 巨大 PR への分岐)
3. **事前確認**: PR テンプレートへの準拠チェック(PR 属性ごとに基準、すべての PR に一律適用)
4. **カバレッジ宣言の出力**(読んだ / 読んでいないファイルとその理由)
5. **モード別レビュー**: 5 視点(ハイブリッド時は **全 10 視点を独立に**)、各指摘に **信頼度 / 根拠視点・根拠項目** を必須付与
6. **総合評価**: 判定とサマリーを出力(起点 SHA + コメント URL 付き、信頼度別の集計含む)
7. **メタフィードバック節**: ルール自身への改善提案を末尾に記載

> **注意**: 事前確認で Critical が見つかった場合、コードレビューを中断して PR 説明の修正を求めます。

### モード明示指定

```
/review code      # コードレビューモードを明示
/review design    # 設計ドキュメントレビューモードを明示
```

### 再レビューモード(`--since`)

修正反映後の差分のみを再レビューします:

```
/review --since=4365084235
```

`<comment-id>` には前回のレビューコメントの GitHub ID を指定。Claude は前回指摘 → 解決状況を判定し、未解決・新規問題のみ報告します(全 14 件 → 解決 14 件のような形でサマリ提示)。

### 特定の視点だけ依頼する場合

```
/review セキュリティ視点だけレビューしてください
/review 開発者視点と QA 視点でレビューしてください
/review design ハルシネーション検出視点だけ
```

### 特定ファイルを指定する場合

```
/review src/gmail/client.py のレビューをしてください
/review design aidlc-docs/inception/application-design/ だけ
```

---

## モード判定アルゴリズム

PR のモードは **2 段階** で判定されます。段階 1(PR 著者の自己宣言)で確定すれば段階 2(自動判定)はスキップされます。

### 段階 1: PR 著者の自己宣言属性を優先採用

PR 説明欄(`gh pr view --json body`)冒頭の **「PR 属性」** セクションでチェックされた `changes-*` 属性から判定:

- `changes-code` / `changes-infra` / `changes-config` → **code 属性**
- `changes-docs` / `changes-aidlc-stage` → **design 属性**

属性集合から **code モード / design モード / ハイブリッドモード** を確定。属性が一つもチェックされていない、または `changes-*` セクション自体が無い場合は段階 2 にフォールバック。

### 段階 2: ファイルベースの自動判定(フォールバック)

`git diff main...HEAD --name-only` で変更ファイル一覧を取得し、以下で判定します:

1. 以下のいずれかに該当するファイルがあれば **code 属性あり**
   - 実装言語ファイル: `.rs` / `.ts` / `.tsx` / `.js` / `.jsx` / `.py` / `.go` / `.java` / `.rb` / `.kt` / `.swift` / `.cs` / `.cpp` / `.c` / `.h` / `.scala` / `.php`
   - 依存マニフェスト: `package.json` / `Cargo.toml` / `go.mod` / `pyproject.toml` / `Gemfile` / `pom.xml` / `requirements.txt`
   - ロックファイル: `package-lock.json` / `Cargo.lock` / `poetry.lock` / `Gemfile.lock` / `yarn.lock` / `pnpm-lock.yaml`
   - CI / リポジトリ設定: `.github/workflows/`(ワークフロー定義のみ。`.github/PULL_REQUEST_TEMPLATE/` および `.github/pull_request_template.md` はテンプレート文書のため code 属性に含めない — 段階 1 で `changes-config` がチェックされていれば code 属性として扱われる)
   - インフラ定義: `*.tf` / `*.yaml`(k8s 等)/ `Dockerfile` / `wrangler.toml` / `compose.yaml`
   - DB / 運用スクリプト: `*.sql`(マイグレーション)/ `*.sh` / `*.bash` / `Makefile`
2. 変更が `*.md` のみ、または `docs/` / 設計文書ディレクトリ(本リポジトリでは `aidlc-docs/` 配下) / `.github/PULL_REQUEST_TEMPLATE/` / `.github/pull_request_template.md` のみであれば **design 属性あり**
3. 判定結果:
   - **code 属性のみ** → **code モード**(code 5 視点)
   - **design 属性のみ** → **design モード**(design 5 視点)
   - **両方(ハイブリッド PR)** → **ハイブリッドモード**: code 5 視点 + design 5 視点 = **全 10 視点を独立に実施**(視点間で観点が重複しても統合せず、相互検証として両方記載)

判定結果は最初のメッセージで明示されます。段階 1 / 段階 2 のどちらで確定したかも併記されます(例: 「モード判定: ハイブリッドモード — 段階 1(自己宣言)で `changes-code + changes-docs + changes-config` を採用、全 10 視点を独立実施」)。

---

## PR 規模・形態の判定

レビュー実行前に PR の規模(変更行数)と形態(コミット種別)を判定し、必要に応じて分岐します。

### 規模軸

| 規模 | 行数 | 動作 |
|------|------|------|
| 小 | < 200 | 標準レビュー |
| 中 | 200〜1,000 | 標準レビュー |
| 大 | 1,000〜5,000 | 標準 + 「集中視点」を冒頭で宣言、関連性の弱いファイルは抜粋確認可 |
| 超大 | > 5,000 | 冒頭で 🟡 Warning「PR 分割推奨」を提示した上でレビュー実施 |

### 形態軸

| 形態 | 検出条件 | 動作 |
|------|---------|------|
| マージコミットのみ PR | 全コミットがマージコミット | レビューせず「no reviewable change」を出力して早期終了 |
| リバート PR | コミット件名が `Revert "..."` または `revert:` | リバート対象との差分を別途確認(手動再導入の有無) |
| ドラフト PR | `gh pr view --json isDraft` が true | 事前確認の必須項目を推奨に緩和、視点レビューは標準実施 |
| 空 PR | 変更ファイル 0 件 | レビュー対象なし、Note のみ返答 |

---

## カバレッジ宣言(出力先頭で必須)

`✅ 問題なし` の信頼性を担保するため、Claude は出力の先頭で **何を読み / 読まなかったか** を必ず明示します。

```markdown
## 📐 カバレッジ宣言

- 総変更行数: 320 行(規模: 中)
- 変更ファイル: 8 件
- 通読: 6 件
- 抜粋確認: 2 件(`large-fixture.json` 等は grep ベース)
- 未確認: 0 件
- N/A 視点項目: 「依存パッケージ」(本 PR は Markdown のみ、対象なし)
- 発動した規模・形態分岐: なし
```

これにより `✅ 問題なし` が「見て問題なし」なのか「見ていないから何も言えない」なのかが追跡可能になります。

---

## 事前確認

事前確認は **すべての PR に一律適用** される 2 種類のチェックで構成されます:

1. 📋 **PR テンプレート準拠** — PR 説明欄の記入状況の静的検査(モード判定の対象)
2. 📝 **コミットメッセージ規約準拠** — 全 commit のメッセージ形式の機械検査(モード判定の対象外、全 PR で必須)

### 📋 事前確認 1: PR テンプレート準拠

コードレビューの前に、PR の説明文が `.github/pull_request_template.md` に沿って記入されているかを確認します。

#### PR 属性ごとの確認基準(属性 OR 集合)

PR テンプレート冒頭の **「PR 属性」** セクションでチェックされた属性すべてに対して、各属性の確認基準を OR で適用します(複数チェック時はいずれか厳しい方)。属性未指定の場合はデフォルトで **changes-code** とみなします。

| 確認項目 | changes-code | changes-docs | changes-infra | changes-config | changes-aidlc-stage |
|---------|:-:|:-:|:-:|:-:|:-:|
| 概要 | 🔴 必須 | 🔴 必須 | 🔴 必須 | 🔴 必須 | 🔴 必須 |
| 変更内容 | 🔴 必須 | 🔴 必須 | 🔴 必須 | 🔴 必須 | 🔴 必須 |
| 関連 Issue / PR / コメント | 🟡 推奨 | 🟡 推奨 | 🟡 推奨 | 🟡 推奨 | 🟡 推奨 |
| 動作確認 | 🔴 必須 | N/A | 🔴 必須 | 🔴 必須(CI 結果) | 緩和 |
| **上流成果物との整合性確認** | N/A | N/A | N/A | N/A | 🔴 必須 |
| レビュー観点 | 🟡 推奨 | 🟡 推奨 | 🟡 推奨 | 🟡 推奨 | 🟡 推奨 |

> **changes-aidlc-stage 属性の特例**: AIDLC ワークフローのステージ完了 PR は本質的に成果物がドキュメントのみであり「ローカル動作確認」が意味をなしません。代わりに **上流成果物(`requirements.md` / `stories.md` 等)との整合性確認** を必須としています。

### 📝 事前確認 2: コミットメッセージ規約準拠

`git log main..HEAD --format='%h %s'` で取得した全 commit のメッセージ件名が、プロジェクト規約 `<type>: <日本語メッセージ>` または `<type>(<scope>): <日本語メッセージ>` 形式に合致するか確認します。本リポジトリの許容 type は `CLAUDE.md` および `.github/workflows/commitlint.yml` 参照。

**モード判定の対象外**(全 PR で必須)。`commitlint` CI と二重チェックですが、CI 結果待ちなしでレビュアーが手元で発見できれば修正サイクルが短縮されます(透明性原則 (a) と運用効率の両立)。

**重要度**: 規約違反 1 件以上 → 🔴 Critical(commit メッセージは履歴改変なしには修正不能のため、レビューブロックして修正フローを要求)。

Critical が 1 件でもある場合(事前確認 1・2 のいずれか)、Claude はコードレビューを中断し PR 説明 / コミットメッセージの修正を促します。

---

## code モード: 5 視点

### 👔 プロダクトオーナー視点

ビジネス価値・要件の整合性を確認します。

**主な確認内容**:
- 実装が要件・Issue の意図を正確に実現しているか
- スコープクリープがないか
- ユーザーへの影響が明確か
- AI がビジネス意図を誤解した実装をしていないか
- **段階間整合性**(上流文書を持つ PR の場合): 上流文書への逆波及更新が必要か
- **構築可能性**(設計変更を伴う場合): 後続実装段階で詰まる未確定事項に「TBD / 解決時期 / 解決責任者」が明示されているか

### 🧪 品質保証 (QA) 視点

テスト品質とリグレッションリスクを確認します。

**主な確認内容**:
- ユニットテスト・統合テストが追加・更新されているか
- エッジケース(null・境界値・タイムアウト等)がカバーされているか
- **AI ハルシネーション検出**: 「それらしいが間違った」ロジックがないか
- 既存テストへの影響

### 💻 開発者視点

コード品質・設計・保守性を確認します。[Google Engineering Practices](https://google.github.io/eng-practices/review/reviewer/looking-for.html) の 10 カテゴリ準拠。

**主な確認内容**:
- 設計の適切さ(過剰な抽象化・AI による不要コードの排除)
- 単一責任・DRY 原則
- 命名・コメントの質
- エラーハンドリング・パフォーマンス
- コミットメッセージ規約の遵守

### 🔒 セキュリティ視点

AI 生成コード特有のリスクを含むセキュリティ脆弱性を確認します。

> **背景**: Veracode 2025 レポートによると、AI 生成コードは人間のコードより **2.74 倍** 脆弱性が多く、検証した 200 以上の AI 開発アプリの **91.5%** にハルシネーション由来の脆弱性が存在しました。

**主な確認内容**:
- シークレット・API キーのハードコード
- 認証・認可ロジックの正確性(Broken Object-Level Authorization)
- インジェクション(SQL・OS・テンプレート)
- **Gmail 等 OAuth 連携のスコープが最小権限か**
- **依存パッケージのハルシネーション**

### 📦 依存・保守視点

依存関係の健全性と長期保守性を確認します。

**主な確認内容**:
- 新規パッケージ追加の正当性
- ロックファイルの更新状況
- 既知の脆弱性(npm audit / pip audit / cargo audit 等)
- 環境変数追加等の設定変更のドキュメント反映

---

## design モード: 5 視点

設計ドキュメント(Markdown 成果物・設計文書ディレクトリ配下の中間生成物)に対するレビュー視点。コードを直接対象としません。

### 🧭 要件整合性視点(段階間整合性含む)

**上流文書**(要件 / ペルソナ / ストーリー等)との整合性を確認します。

**主な確認内容**:
- 上流文書の意図と一致しているか
- **段階間整合性**: 本 PR で確定した判断により上流文書の **逆波及更新** が必要か
- スコープクリープ(要求外の設計拡張)がないか
- 用語・命名が上流と一貫しているか

> **段階間整合性の例**: 設計段階で機能 X を Phase 2 移行と決定した場合、要件文書の MVP スコープ列挙も同期更新が必要。これを怠ると 3 文書間で齟齬が生じます。

### 🧩 設計コヒーレンス視点(内部矛盾)

同 PR 内の内部矛盾を検出します。

**主な確認内容**:
- 同じ事象が複数箇所で別事実として記述されていないか(例: シーケンス図 ↔ Queue 構成表)
- 設計判断(Plan の Q&A 回答)と実装方針が一致しているか
- 章節間の参照が崩れていないか
- 表記揺れ(同一概念に異なる名前)がないか
- エラー型・データ型・API スキーマの食い違い

### 🔍 ハルシネーション検出視点(設計版)

設計ドキュメント特有のハルシネーションを検出します。AI が生成した設計ドキュメントには **「コード上では明らかに分からないが設計レベルでよく出る誤り」** があり、本視点ではそれを系統的に検出します。

**4 つの検出パターン**:

| パターン | 例 | 検出方法 |
|---------|---|---------|
| **数値整合性** | 「全 51 コンポーネント」と書きつつ表合計が 49 | 冒頭サマリ ↔ 表合計 ↔ 各セクションの数値を突合 |
| **ID 参照整合性** | 存在しない Story `F-15` を参照(実在は F-14 まで) | 参照されている全 ID を上流文書で `grep` 確認 |
| **重複記述の事実一致** | 同じ事象を 3 箇所で異なる事実として記述 | 同じキーワードで複数箇所の記述を比較 |
| **列挙の網羅性** | 「10 ユースケース」と書いて 9 個しか定義されていない | 完了基準で挙げた要素が本文に存在するか確認 |

#### 機械的検証手順(grep ベース)

ID 参照整合性 / 数値整合性は手作業だと見落としやすいため、以下の grep ベース手順で機械的に検証することを推奨します:

```bash
# 設計側で参照されている全 ID を抽出
grep -ohE "F-[0-9]+|U[0-9]+(-[A-Z0-9]+)+" path/to/design/*.md | sort -u > /tmp/design_ids.txt

# 上流文書で実在する ID を抽出
grep -ohE "F-[0-9]+|U[0-9]+(-[A-Z0-9]+)+" path/to/upstream/*.md | sort -u > /tmp/upstream_ids.txt

# 設計側にのみ存在 = ハルシネーション候補
comm -23 /tmp/design_ids.txt /tmp/upstream_ids.txt

# 「全 N 件」宣言と CREATE TABLE 等の実数の突合
grep -cE "^CREATE TABLE" path/to/schema.md
```

### 🔒 セキュリティ視点(設計レベル)

設計段階で識別すべき脅威モデル・セキュリティ要件を確認します。

**主な確認内容**:
- 公開エンドポイントすべてに認証・認可方針が明示されているか
- 機密データ(トークン・PII)の保管・暗号化方針があるか
- BOLA / IDOR 防止策がリソース参照系 API で言及されているか
- 監査ログ・改竄防止の機械的保証(トリガー・append-only 等)が指定されているか
- OAuth スコープ・最小権限原則の言及
- 設計レベルの脅威モデリング(STRIDE 等)

### 🏗️ 構築可能性視点

**後続ステージ**(詳細設計 / 実装等)で本設計が **実装可能** か、未確定事項が後続を阻害しないかを確認します。

**主な確認内容**:
- **未確定事項のリスト化**: タイムアウト・リトライ回数・コスト上限値などが「後続ステージで確定」と明示されているか
- **拡張性ポート**: 抽象化レイヤが将来の仕様変更を本当に吸収できる設計か
- **コスト・性能の達成可能性**: NFR 達成の **設計目標値の枠組み** が示されているか
- **テスト戦略との接続**: モック境界・PBT 対象・E2E スコープ

> **設計ドキュメントでは「依存・保守視点」が機能しないため、構築可能性視点で代替** します。

---

## 重要度レベルと判定基準マトリクス

### 共通レベル定義

| アイコン | レベル | 対応方針 |
|---------|--------|---------|
| 🔴 | **Critical** | マージ前に必ず修正(信頼度 🎯/🤔 のみブロック、❓ Speculative は要検証扱い) |
| 🟡 | **Warning** | 修正を強く推奨。理由があれば許容 |
| 🟢 | **Suggestion** | 改善提案。対応は任意 |
| 💡 | **Note** | 情報共有・補足。対応不要 |

### 信頼度(Confidence)軸 — 必須

各指摘には重要度と **直交する信頼度** を必ず併記します:

| アイコン | レベル | 意味 |
|---------|--------|------|
| 🎯 | **High** | 該当箇所を直接読み、外部仕様 / 標準 / 上流文書と突合済み。誤指摘の可能性は低い |
| 🤔 | **Medium** | 主要箇所は確認したが推測や前提が一部含まれる。著者側で再確認推奨 |
| ❓ | **Speculative** | 仮説段階。レビュアー側で検証不能で、PR 著者の検証要請を含む |

**信頼度と総合評価の対応**:

- 🔴 Critical / ❓ Speculative の指摘は **マージブロックではなく「要検証」扱い**(著者が反証できればそのまま進めて良い、判定は `💬 Comment`)
- 🔴 Critical / 🎯 High または 🤔 Medium が 1 件でもあれば `🔄 Request Changes`
- AI レビュアー特有のハルシネーション Critical で誤マージブロックを起こすことを防ぐ仕組みです

### マスター文書 vs 派生文書の判定原則

要素の **欠落 / 不整合** は、それが起きた場所がマスター文書か派生文書かで重要度を切り分けます:

| 場所 | 欠落・不整合の重要度 |
|------|---------------------|
| **マスター文書**(責務を最初に定義する文書)で欠落 | 🔴 **Critical**(ソース・オブ・トゥルースの欠陥) |
| **派生文書**(図示・要約・引用)のみで欠落 | 🟡 **Warning**(マスターには存在し、派生のみ未追従) |
| マスター ↔ 派生で **食い違い** | 🔴 **Critical**(実装で必ず齟齬) |

### 拡張オプトイン連動ルール

リポジトリで Extension が **Enabled** の場合、その Extension が要求する成果物の **欠落** は **🔴 Critical に引き上げ** ます。
- 例: `security-baseline` Enabled かつ STRIDE 実施結果が設計成果物に存在しない → Critical
- 例: `property-based-testing` Enabled かつ PBT 対象の特定が無い → Critical

Extension の有効状態は本リポジトリでは `aidlc-docs/aidlc-state.md` の `## Extension Configuration` で確認します。

### code モード判定例

| 種類 | 🔴 Critical | 🟡 Warning | 🟢 Suggestion |
|------|-----------|-----------|--------------|
| シークレット | API キーのハードコード | `.env.example` 未更新 | コメントの redact ガイド追記 |
| 認証・認可 | BOLA を引き起こすクエリ | 認可粒度が荒い | テスト追加 |
| インジェクション | SQL/OS コマンドへの文字列結合 | エスケープ漏れの可能性 | パラメータバインドの統一 |
| テスト | 必須経路のテスト未追加 | エッジケース未カバー | テスト命名改善 |
| 依存 | ハルシネーションパッケージ参照 | 既知脆弱性のあるバージョン | バージョン固定の方針 |

### design モード判定例

| 種類 | 🔴 Critical | 🟡 Warning | 🟢 Suggestion |
|------|-----------|-----------|--------------|
| 内部矛盾 | 同じ事象が 2 箇所で別事実(実装で必ず齟齬) | 表記揺れ・粒度不一致 | 補足が望ましい |
| ID 参照 | 存在しない Story / 型 を参照(ハルシネーション) | リンク先未生成 | 別表現が好ましい |
| 数値整合性 | サマリ数 ≠ 詳細合計 | 概算と精密値の混在 | 単位明示の不足 |
| 上流不整合 | 要件と矛盾する設計 | 要件記述が陳腐化 | 用語のゆらぎ |
| 完全性 | マスター文書で必須要素が欠落 | 派生文書のみで欠落 | 図示があると親切 |
| セクション参照 | 「セクション N 参照」とあるが存在しない | リンク先がアンカー違い | 参照表現の統一 |
| セキュリティ | 公開エンドポイントの認証方針未記述 | 暗号化方針が部分的 | 脅威モデル参照リンク |
| 構築可能性 | 後続実装が不能な未確定事項 | 「TBD」のまま放置(解決時期なし) | 上限値の暫定示唆 |
| 拡張連動 | Enabled Extension の必須成果物欠落 | Extension ガイドに沿わない記述 | 関連リンク追加 |

---

## 出力フォーマット

各指摘には以下を **必須** で付与します:

- **信頼度アイコン**(🎯 / 🤔 / ❓)
- **根拠視点**: その指摘がどの視点の確認項目から導かれたか
- **根拠項目**: 視点内のチェック項目をそのまま引用

### 単一箇所の指摘

```
### [視点名] レビュー

#### 🟡 エッジケースのテストが不足しています 🎯 High
**根拠視点**: code 視点 2 品質保証 (QA)
**根拠項目**: 「エッジケース(境界値・null・空配列・タイムアウト等)がカバーされているか」
**ファイル**: `src/gmail/client.py`
**セクション**: `class GmailClient`
**行**: L45-L60
**指摘**: Gmail API がレート制限エラーを返した場合のテストがありません
**提案**: `RateLimitError` を mock して retry 処理が動作することを確認するテストを追加してください
```

### 複数箇所をまたがる整合性指摘(design モードで頻出)

```
#### 🔴 A-3(decision)の流れ先が 3 箇所で異なる事実 🎯 High
**根拠視点**: design 視点 2 設計コヒーレンス
**根拠項目**: 「同じ事象を複数箇所で説明している場合、事実が一致しているか」
**対象箇所**(複数):
- `services.md` セクション 3「Queue 構成表」(L82, L83)
- `services.md` シーケンス図 1(L36-49)
- `services.md` シーケンス図 2(L54-72)
**指摘**: 同一事象「A-3(decision)が後続ステップに渡る経路」が、表 / 第 1 図 / 第 2 図で 3 通りに記述されている。実装時に必ず齟齬する
**提案**: 真の方針を 1 つ確定し、3 箇所を一致させる
```

### 仮説指摘の例(ハルシネーション抑制)

```
#### 🔴 認可チェックが欠落している可能性 ❓ Speculative
**根拠視点**: code 視点 4 セキュリティ
**根拠項目**: 「認可 (BOLA): リソース ID で参照する処理で、呼び出し元の所有権確認があるか」
**ファイル**: `src/api/orders.py:42`
**指摘**: `get_order(order_id)` で呼び出し元の所有権を確認しているか、本コードからは判断できません(`auth_middleware` が他レイヤで実施されている可能性)
**提案**: ミドルウェア構成を著者から共有願います。実施箇所が確認できれば本指摘は撤回します
```

→ Critical かつ ❓ Speculative のため判定は `💬 Comment`(マージブロックではなく要検証要請)

### 指摘がない視点(✅ 問題なし)— チェック項目全件の ✓/N/A 必須化

該当視点のチェック項目 **全件について `✓ / N/A(理由)` を明示** します。過剰でも全件カバレッジを優先します:

```
### 🔒 セキュリティ視点 レビュー

✅ 問題なし(チェック項目 8/8 件確認)
- ✓ シークレット: ハードコードされた API キー / トークンなし
- ✓ 認証: §4.2 認証表で全公開エンドポイントの認証方式記載
- ✓ 認可 (BOLA): §4.2 で user_id パラメータ権限チェック必須を明記
- N/A インジェクション: 本 PR は Markdown のみ、SQL/OS コマンド出力なし
- N/A 入力検証: 本 PR は Markdown のみ、外部入力処理なし
- N/A 依存パッケージ: 本 PR は Markdown のみ、新規依存追加なし
- ✓ OAuth スコープ: §7.4 OAuth スコープ表で最小権限を明示
- ✓ エラーレスポンス: §5.2 エラーハンドリング表でスタックトレース非露出を確認
```

このフォーマットは再レビュー時の差分判定にも有効です(✅ 確認済とした根拠が変更されていなければ前回判定を維持できる)。

---

## 総合評価例

```
## 総合評価

**判定**: 🔄 Request Changes

**サマリー**(重要度 × 信頼度):
- 🔴 Critical: 2 件(うち 🎯 High 1 / 🤔 Medium 1)
- 🟡 Warning: 3 件(うち 🎯 High 2 / 🤔 Medium 1)
- 🟢 Suggestion: 0 件
- 💡 Note: 1 件

**優先対応項目**(🎯 High / 🤔 Medium のみ):
- `config/settings.py` の API キーをシークレット管理に移行してください

**要検証項目**(❓ Speculative、判定に影響なし):
- なし

**起点 SHA**: `129dc48`(本レビュー時点の HEAD、次回 `--since` 起動時の差分起点)
**レビュー反映確認用 URL**: https://github.com/owner/repo/pull/N#issuecomment-XXXXXXX
  (投稿後にコメントを編集して URL を埋める運用。コメント ID は URL 末尾から取得可能)
```

---

## 🪞 メタフィードバック節(出力末尾で必須)

すべてのレビュー出力の末尾に、ルール適用中の気づきを構造化収集します。空でも「特になし」と明示記述します(空の宣言と未記載は別物として扱う)。

```markdown
## 🪞 本ルールへのフィードバック

- 判断に迷った箇所: [例: 「視点 3 と視点 2 で同一指摘が立った、振り分け基準が曖昧」/ なしなら「なし」]
- ルール文言が曖昧だった箇所: [例: 「カバレッジ宣言の "抜粋確認" の定義が不明瞭」/ なしなら「なし」]
- 想定外のケース: [例: 「PR が複数の独立した変更を含み、モードが揺れた」/ なしなら「なし」]
- 改善提案: [例: 「視点 3 に "略語整合性" のチェック項目を追加してはどうか」/ なしなら「なし」]
```

このフィードバックは PR の総合評価判定には影響しませんが、ルール改修時の主要情報源として PR 履歴から収集されます。

---

## 再レビュー(`--since`)モード

修正反映後の差分のみを再レビューするためのモード。「指摘 → 修正 → 再レビュー」のサイクルを効率化します。

### 起点 SHA の推定アルゴリズム

GitHub の **issue comment** には `commit_id` フィールドが無いため、以下の優先順位で起点 SHA を推定します:

1. **前回コメント本文の `起点 SHA` 表記を優先**(上記の総合評価フォーマットで埋め込まれている前提)
2. 表記が無い場合、**前回コメントの `created_at` 時刻** を取得し、PR コミット履歴のうち `commit.committer.date <= comment.created_at` を満たす **最新コミット** を起点 SHA とする
3. それも不能な場合、PR の **最初のコミット** を起点とする(全件再レビュー)

### 動作

1. 引数 `--since=<comment-id>` から **起点となる前回レビューコメント** を取得
2. 上記アルゴリズムで起点 SHA を推定し、`<起点 SHA>..HEAD` の **差分 commit 一覧** を取得
3. 前回コメントの **全指摘を一覧化**
4. 各指摘について **解決状況を判定**:
   - ✅ 解決
   - ⚠️ 部分解決
   - ❌ 未解決
   - ❓ 確認不能(該当箇所が大幅に変わって直接比較できない)
5. **新規検出された問題** がある場合、以下の 3 分類で出自を必ず明示:
   - 🆕 **New**: 本区間(起点 SHA..HEAD)のコミットで導入された問題
   - 🙇 **Missed**: 起点 SHA 時点で既に存在していた、前回見落とし(レビュアーの自己申告)
   - 🔄 **Resurfaced**: 一度修正されたが本区間で再発した(リバート / 再導入の可能性)
6. 総合評価で「前回 Critical N 件 → 今回 0 件」のような **解決ステータスサマリ** に加え、新規検出の分類別件数(🆕 X / 🙇 Y / 🔄 Z)も提示

> **新規検出の 3 分類はレビュアー(人間/AI 問わず)の品質指標として独立価値があります**。AI レビュアーの場合、`🙇 Missed` は前回プロンプト時の見落とし率として観測されます。

### 出力例

```
## 📐 カバレッジ宣言
[初回レビューと同様]

### 🟢 修正確認

**起点 SHA**: `5709b1b`(推定根拠: 前回コメント本文)
**差分コミット**: `5709b1b..129dc48`(3 コミット)

#### 前回指摘の解決状況

##### 🔴 Critical(3 件 / 全解決)

| ID | 検証結果 |
|----|---------|
| **C1** A-3 流れ先 3 説 | ✅ 完全解決。Queue 表 + 2 シーケンス図が一致 |
| **C2** F-15 ハルシネーション | ✅ F-13 に修正済 |
| **C3** A-10 設計片手落ち | ✅ 5 ファイル横断で完成 |

#### 新規検出(🆕 New / 🙇 Missed / 🔄 Resurfaced)

##### 🟡 Warning コメント不足 🤔 Medium 🆕 New
**出自分類**: 🆕 New(本区間で導入)
**根拠視点**: code 視点 3 開発者
**根拠項目**: 「コメント: 「何を」でなく「なぜ」を説明するコメントが書かれているか」
[以下、初回レビューと同形式]

## 総合評価
判定: ✅ Approve

サマリー(再レビュー版):
- 前回指摘の解決状況: 🔴 0/3 / 🟡 0/0 / 🟢 0/0
- 新規検出: 🆕 1 件 / 🙇 0 件 / 🔄 0 件

起点 SHA: `129dc48`(次回参照用)

## 🪞 本ルールへのフィードバック
[初回と同様に必須記載]
```

---

## ファイル構成

```
.claude/
└── commands/
    └── review.md   # /review コマンドの定義(Claudeへの指示)

docs/
└── review-guide.md            # このドキュメント(使い方ガイド)

.github/
├── pull_request_template.md          # デフォルト PR テンプレート(PR 属性チェックボックス)
└── PULL_REQUEST_TEMPLATE/            # 属性別クイックピッカーテンプレート
    ├── code.md
    ├── docs.md
    └── aidlc-stage.md
```

---

## カスタマイズ

### コアルールの再利用(他プロジェクトへの移植)

`.claude/commands/review.md` のコアルール部分は **抽象語彙**(上流文書 / 下流文書 / マスター文書 / 派生文書)で書かれています。AIDLC 以外のワークフロー / 別リポジトリへ移植する場合は、末尾の「AIDLC マッピング」セクションを当該プロジェクトの構造に合わせて書き換えれば、コアルールはそのまま再利用できます。

### プロジェクト固有の観点を追加する場合

`.claude/commands/review.md` を編集することでレビュー観点を調整できます。

```markdown
## 視点 6: 🗓️ MC 業務固有視点

**確認項目**:
- [ ] Gmail 送信処理に誤送信防止の確認ステップがあるか
- [ ] 司会スクリプトの文言に問題がないか
```

新しい観点はモード(code / design)のいずれかに紐付けるか、新モードを追加してください。
